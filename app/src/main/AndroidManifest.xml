<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StoryTeller"
        android:windowLayoutInDisplayCutoutMode="shortEdges">
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true"
            android:windowLayoutInDisplayCutoutMode="shortEdges">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.StoryGenerateActivity"
            android:windowLayoutInDisplayCutoutMode="shortEdges" />
        <activity
            android:name=".ui.activity.CharacterActivity"
            android:windowLayoutInDisplayCutoutMode="shortEdges" />
        <activity
            android:name=".ui.activity.PlotTreeActivity"
            android:windowLayoutInDisplayCutoutMode="shortEdges" />
        <activity
            android:name=".ui.activity.MaterialActivity"
            android:windowLayoutInDisplayCutoutMode="shortEdges" />
        <activity
            android:name=".ui.activity.StoryDetailActivity"
            android:windowLayoutInDisplayCutoutMode="shortEdges" />
        <activity
            android:name=".ui.activity.MaterialDetailActivity"
            android:windowLayoutInDisplayCutoutMode="shortEdges" />
        <activity
            android:name=".ui.activity.SettingsActivity"
            android:windowLayoutInDisplayCutoutMode="shortEdges" />
        <activity
            android:name=".ui.activity.VolumeChaptersActivity"
            android:windowLayoutInDisplayCutoutMode="shortEdges" />
    </application>

</manifest>