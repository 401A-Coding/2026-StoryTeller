<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FFFFFF">

    <!-- 顶部标题栏 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:gravity="center_vertical"
        android:background="#1976D2">

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_book_info"
            android:layout_marginEnd="8dp"
            android:contentDescription="小说信息"
            app:tint="#FFFFFF" />

        <TextView
            android:id="@+id/tv_story_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="新小说"
            android:textColor="#FFFFFF"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_marginEnd="4dp" />

        <!-- 切换小说按钮（紧邻标题） -->
        <ImageView
            android:id="@+id/btn_select_story"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_arrow_drop_down"
            android:contentDescription="切换小说"
            android:padding="4dp"
            app:tint="#FFFFFF" />

        <!-- 占位空间，将关闭按钮推到右侧 -->
        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <!-- 关闭按钮 -->
        <ImageView
            android:id="@+id/btn_close_panel"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_close"
            android:contentDescription="关闭"
            android:padding="4dp"
            app:tint="#FFFFFF" />
    </LinearLayout>

    <!-- Tab导航 -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tab_story_info"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:tabMode="fixed"
        app:tabGravity="fill"
        app:tabIndicatorColor="#1976D2"
        app:tabSelectedTextColor="#1976D2"
        app:tabTextColor="#666666" />

    <!-- 内容区域 -->
    <FrameLayout
        android:id="@+id/layout_panel_content"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <!-- 设定面板 -->
        <FrameLayout
            android:id="@+id/panel_setting"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="visible" />

        <!-- 大纲面板 -->
        <FrameLayout
            android:id="@+id/panel_outline"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone" />

        <!-- 目录面板 -->
        <LinearLayout
            android:id="@+id/panel_toc"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:visibility="gone">

            <ScrollView
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1">

                <LinearLayout
                    android:id="@+id/layout_toc"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:padding="16dp" />
            </ScrollView>

            <!-- 页码指示器 -->
            <HorizontalScrollView
                android:id="@+id/horizontal_page_indicator"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="16dp"
                android:layout_marginBottom="12dp"
                android:scrollbars="none">

                <LinearLayout
                    android:id="@+id/layout_page_indicator"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal" />
            </HorizontalScrollView>
        </LinearLayout>

        <!-- 文档面板 -->
        <FrameLayout
            android:id="@+id/panel_docs"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone" />

    </FrameLayout>

</LinearLayout>
