# AI 智能体使用指南

## 🎉 集成完成！

AI 智能体功能已成功集成到故事生成页面。现在你可以通过自然语言轻松编辑小说！

---

## 🚀 快速开始

### 1. **打开故事生成页面**
- 从首页或书架页选择一本小说
- 进入编辑模式

### 2. **打开 AI 助手面板**
- 点击右上角的机器人图标 🤖
- AI 助手面板会从右侧滑出

### 3. **启用智能体模式**
- 在 AI 助手面板顶部，找到"智能体模式"开关
- 打开开关 → 启用智能体模式
- 关闭开关 → 普通聊天模式

---

## 💡 使用示例

### ✅ 智能体模式（推荐用于编辑）

#### **添加章节**
```
用户输入："帮我添加一个悬疑章节，讲述主角发现神秘宝藏"

AI 执行：
✓ 创建新章节
✓ 生成章节标题："神秘宝藏"
✓ 生成章节内容
✓ 自动保存到数据库
✓ 刷新 UI 显示新章节

AI 回复："已成功添加章节：神秘宝藏"
```

#### **情节建议**
```
用户输入："接下来情节应该怎么发展？"

AI 执行：
✓ 分析当前故事结构
✓ 生成多条情节建议

AI 回复：
"情节建议：
1. 引入一个反派角色
2. 设置时间限制增加紧张感
3. 让主角面临道德抉择"
```

#### **角色创作**
```
用户输入："给主角起个名字和背景故事"

AI 执行：
✓ 生成角色信息

AI 回复：
"角色建议：
姓名：林风
背景：一名年轻的考古学家，对古代文明充满好奇..."
```

#### **普通问答**
```
用户输入："你觉得这个故事有趣吗？"

AI 执行：
✓ 分析问题
✓ 给出评价和建议

AI 回复：
"这个故事很有潜力！主角的性格塑造很生动，
建议在后续章节中..."
```

---

### 💬 普通聊天模式

适用于：
- 生成完整的故事内容
- 创意写作灵感
- 不涉及具体编辑操作

```
用户输入："写一个科幻短篇故事"

AI 执行：
✓ 生成完整故事
✓ 自动保存到书架

AI 回复：
"《星际迷航》
在遥远的未来..."
```

---

## 🎯 支持的操作类型

| 操作 | 说明 | 示例 |
|------|------|------|
| `add_chapter` | 添加新章节 | "添加一个恐怖章节" |
| `edit_chapter` | 编辑章节内容 | "修改第一章的内容" |
| `generate_plot` | 生成情节建议 | "接下来该怎么发展？" |
| `create_character` | 创建角色 | "创建一个反派角色" |
| `answer_question` | 回答问题 | "这个故事怎么样？" |

---

## ⚙️ 工作原理

```
用户输入
  ↓
智能体模式开启？
  ├─ 是 → AI 分析意图 → 返回结构化命令 → 执行操作 → 刷新 UI
  └─ 否 → 普通聊天 → 生成文本 → 保存故事
```

### **智能体模式流程：**

1. **构建上下文**
   - 小说标题、类型
   - 卷章结构
   - 最近500字符内容

2. **AI 分析**
   - 理解用户意图
   - 选择合适的操作类型
   - 生成必要的参数

3. **执行命令**
   - 解析 JSON 命令
   - 调用对应的处理方法
   - 更新数据库

4. **反馈结果**
   - 显示执行结果
   - 刷新 UI（如有修改）
   - 通知用户

---

## 🔧 技术细节

### **文件修改清单**

1. ✅ [ApiClient.java](file:///D:/Tsinghua/projects/26_1_spring/android/2026-StoryTeller/app/src/main/java/com/example/storyteller/data/remote/ApiClient.java)
   - 新增 `processAgentCommand()` 方法
   - 新增 `AgentCommand` 模型
   - 新增 `AgentCallback` 接口

2. ✅ [AgentCommandExecutor.java](file:///D:/Tsinghua/projects/26_1_spring/android/2026-StoryTeller/app/src/main/java/com/example/storyteller/utils/AgentCommandExecutor.java)
   - 新建命令执行器类
   - 实现各操作的处理逻辑
   - 提供上下文构建工具

3. ✅ [StoryGenerateActivity.java](file:///D:/Tsinghua/projects/26_1_spring/android/2026-StoryTeller/app/src/main/java/com/example/storyteller/ui/activity/StoryGenerateActivity.java)
   - 添加智能体成员变量
   - 修改 `sendMessage()` 支持双模式
   - 新增 `refreshStoryView()` 方法
   - 绑定智能体模式开关

4. ✅ [activity_story_generate.xml](file:///D:/Tsinghua/projects/26_1_spring/android/2026-StoryTeller/app/src/main/res/layout/activity_story_generate.xml)
   - 在 AI 面板添加智能体模式开关

---

## ⚠️ 注意事项

### **1. API 成本**
- 智能体模式每次调用消耗更多 token
- 建议合理使用，避免频繁调用
- 可以考虑添加调用频率限制

### **2. 编辑模式要求**
- 智能体模式仅在**编辑现有小说**时可用
- 新建故事时使用普通聊天模式

### **3. 错误处理**
- AI 可能返回格式错误的 JSON
- 系统会自动降级为普通聊天
- 网络错误会显示友好提示

### **4. 数据安全**
- 所有操作由前端执行
- AI 只返回命令，不直接修改数据
- 可以添加确认机制（待实现）

---

## 🚀 未来扩展

### **可添加的功能：**

1. **确认机制**
   ```java
   // 在执行重要操作前弹出确认对话框
   new AlertDialog.Builder(this)
       .setTitle("确认操作")
       .setMessage("AI 建议添加章节，是否执行？")
       .setPositiveButton("执行", ...)
       .show();
   ```

2. **撤销功能**
   ```java
   // 记录操作历史，支持撤销
   private Stack<UndoAction> undoStack = new Stack<>();
   ```

3. **批量操作**
   ```json
   {
     "actions": [
       {"action": "add_chapter", ...},
       {"action": "edit_chapter", ...}
     ]
   }
   ```

4. **更多命令类型**
   - `delete_chapter` - 删除章节
   - `merge_chapters` - 合并章节
   - `rewrite_content` - 重写内容
   - `suggest_title` - 建议标题
   - `analyze_structure` - 分析结构

---

## 📊 效果展示

### **界面布局：**

```
┌─────────────────────────────────────┐
│ ← 返回              🤖 AI  💾 保存  │
├─────────────────────────────────────┤
│                                     │
│         小说编辑区域                 │
│                                     │
│   第1卷 · 冒险开始                   │
│     第1章 · 出发                     │
│     [内容编辑器]                     │
│                                     │
│   [+ 添加章节]                       │
│                                     │
│   [+ 添加卷]                         │
│                                     │
└─────────────────────────────────────┘
         ↓ 点击 AI 图标
┌─────────────────────────────────────┐
│ 🤖 AI助手                      [×]  │
│ □ 智能体模式          [开关: ON]    │
├─────────────────────────────────────┤
│                                     │
│  [聊天记录区域]                      │
│  用户：帮我添加一个悬疑章节          │
│  AI：已成功添加章节：神秘宝藏        │
│                                     │
├─────────────────────────────────────┤
│ [输入框...]                  [发送] │
└─────────────────────────────────────┘
```

---

## 🎉 开始使用！

现在你可以：

1. ✅ 打开任意小说进行编辑
2. ✅ 点击 AI 图标打开助手
3. ✅ 启用智能体模式
4. ✅ 用自然语言编辑小说

**试试这样说：**
- "帮我添加一个新章节"
- "接下来情节怎么发展？"
- "给主角起个名字"
- "你觉得这个故事怎么样？"

享受 AI 智能编辑带来的便利吧！🚀
