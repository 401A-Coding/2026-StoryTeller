# 章节移动与合并功能使用指南

## 📖 概述

新增了两个强大的章节管理功能：
1. **移动章节 (move_chapter)** - 在同卷内或跨卷移动章节
2. **合并章节 (merge_chapters)** - 将多个连续章节合并为一个

---

## 🎯 功能一：移动章节

### 基本用法

用户可以通过自然语言让 AI 移动章节：

**示例对话：**
```
用户："把第三章移到第五章后面"
用户："把第一卷的第二章移到第二卷开头"
用户："调整章节顺序，让悬念章节放在最后"
```

### 命令格式

```json
{
  "action": "move_chapter",
  "parameters": {
    "from_volume_id": 1,
    "from_chapter_id": 3,
    "to_volume_id": 2,
    "to_position": 1,
    "insert_after": false
  }
}
```

### 参数说明

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `from_volume_id` | int | 是 | 源卷ID（从1开始） |
| `from_chapter_id` | int | 是 | 源章节ID（从1开始） |
| `to_volume_id` | int | 是 | 目标卷ID（可与源卷相同） |
| `to_position` | int | 是 | 目标位置（插入到第几章之前/之后） |
| `insert_after` | boolean | 否 | 是否插入到目标位置之后（默认true） |

### 使用场景

#### 场景1：同卷内移动
```
用户："把第3章移到第5章后面"

AI 返回：
{
  "action": "move_chapter",
  "parameters": {
    "from_volume_id": 1,
    "from_chapter_id": 3,
    "to_volume_id": 1,
    "to_position": 5,
    "insert_after": true
  }
}

执行结果：✅ 已将章节《XXX》移动到第5章之后
```

#### 场景2：跨卷移动
```
用户："把第一卷第二章移到第二卷开头"

AI 返回：
{
  "action": "move_chapter",
  "parameters": {
    "from_volume_id": 1,
    "from_chapter_id": 2,
    "to_volume_id": 2,
    "to_position": 1,
    "insert_after": false
  }
}

执行结果：✅ 已将章节《XXX》从第1卷移动到第2卷的第1章之前
```

### 实现细节

1. **同卷内移动**：
   - 移除原章节
   - 计算正确的插入位置（考虑原位置对索引的影响）
   - 插入到目标位置
   - 重新编号所有章节

2. **跨卷移动**：
   - 从源卷移除章节
   - 重新编号源卷的后续章节
   - 插入到目标卷的指定位置
   - 重新编号目标卷的后续章节

3. **安全检查**：
   - 验证源卷和目标卷ID的有效性
   - 验证源章节ID的有效性
   - 验证目标位置的合法性
   - 如果已在目标位置，提示无需移动

---

## 🎯 功能二：合并章节

### 基本用法

用户可以通过自然语言让 AI 合并多个连续章节：

**示例对话：**
```
用户："把第三、四、五章合并成一章"
用户："这两个短章节太碎了，合并一下"
用户："把第2到第4章合并"
```

### 命令格式

```json
{
  "action": "merge_chapters",
  "parameters": {
    "volume_id": 1,
    "chapter_ids": [3, 4, 5],
    "new_title": "合并后的新标题",
    "merge_strategy": "concatenate"
  }
}
```

### 参数说明

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `volume_id` | int | 是 | 卷ID（从1开始） |
| `chapter_ids` | List<int> | 是 | 要合并的章节ID列表（必须连续） |
| `new_title` | String | 否 | 合并后的新标题（可选，AI可生成） |
| `merge_strategy` | String | 否 | 合并策略（目前只支持 concatenate） |

### 使用场景

#### 场景1：合并三个章节
```
用户："把第3、4、5章合并"

AI 返回：
{
  "action": "merge_chapters",
  "parameters": {
    "volume_id": 1,
    "chapter_ids": [3, 4, 5],
    "new_title": "冒险之旅",
    "merge_strategy": "concatenate"
  }
}

执行结果：✅ 已成功合并 3 个章节为《冒险之旅》
```

#### 场景2：合并两个短章节
```
用户："第2和第3章太短了，合并一下"

AI 返回：
{
  "action": "merge_chapters",
  "parameters": {
    "volume_id": 1,
    "chapter_ids": [2, 3],
    "new_title": "",  // AI未提供标题，使用默认
    "merge_strategy": "concatenate"
  }
}

执行结果：✅ 已成功合并 2 个章节为《第一章标题（合并）》
```

### 实现细节

1. **验证阶段**：
   - 验证卷ID有效性
   - 验证所有章节ID有效性
   - 验证章节是否连续（必须是连续的ID）
   - 至少需要两个章节才能合并

2. **合并阶段**：
   - 获取所有章节的内容
   - 用 `\n\n` 连接所有内容
   - 如果没有提供新标题，使用第一个章节标题 + "（合并）"

3. **更新阶段**：
   - 从后往前删除原章节（避免索引变化）
   - 在第一个位置插入合并后的新章节
   - 重新编号后续章节
   - 保存更新后的结构

4. **合并策略**（当前实现）：
   - `concatenate`: 直接拼接内容（已实现）
   - `summarize`: AI总结后合并（待实现）
   - `select_best`: 选择最好的部分（待实现）

---

## 💡 AI 提示词示例

在 ApiClient 的系统提示中，已经添加了以下示例：

### 移动章节示例
```json
移动章节（同卷内）：
{
  "action": "move_chapter",
  "parameters": {
    "from_volume_id": 1,
    "from_chapter_id": 3,
    "to_volume_id": 1,
    "to_position": 5,
    "insert_after": true
  },
  "reasoning": "用户想把第3章移到第5章后面"
}

移动章节（跨卷）：
{
  "action": "move_chapter",
  "parameters": {
    "from_volume_id": 1,
    "from_chapter_id": 2,
    "to_volume_id": 2,
    "to_position": 1,
    "insert_after": false
  },
  "reasoning": "用户想把第1卷第2章移到第2卷开头"
}
```

### 合并章节示例
```json
合并章节：
{
  "action": "merge_chapters",
  "parameters": {
    "volume_id": 1,
    "chapter_ids": [3, 4, 5],
    "new_title": "合并后的新标题",
    "merge_strategy": "concatenate"
  },
  "reasoning": "用户想合并第3、4、5章"
}
```

---

## ⚠️ 注意事项

### 移动章节
1. **位置计算**：
   - `to_position` 指的是目标位置的章节编号
   - `insert_after=true` 表示插入到该章之后
   - `insert_after=false` 表示插入到该章之前

2. **边界情况**：
   - 如果目标位置超出范围，会返回错误提示
   - 如果章节已在目标位置，会提示无需移动
   - 跨卷移动时，章节ID会重置

3. **自动重编号**：
   - 移动后会自动重新编号所有受影响的章节
   - 保证每个卷内的章节ID从1开始连续递增

### 合并章节
1. **连续性要求**：
   - 只能合并连续的章节（如 3,4,5）
   - 不能合并不连续的章节（如 2,4,6）

2. **最少数量**：
   - 至少需要两个章节才能合并
   - 如果只提供一个章节ID，会返回错误

3. **内容处理**：
   - 目前只实现直接拼接（concatenate）
   - 章节之间用两个换行符分隔
   - 未来可以实现AI智能总结合并

4. **标题生成**：
   - 如果AI提供了新标题，使用新标题
   - 否则使用第一个章节标题 + "（合并）"

---

## 🧪 测试建议

### 测试用例1：同卷内向前移动
```
初始状态：第1卷有5章 [1, 2, 3, 4, 5]
操作：把第3章移到第1章前面
预期结果：[3, 1, 2, 4, 5]
```

### 测试用例2：同卷内向后移动
```
初始状态：第1卷有5章 [1, 2, 3, 4, 5]
操作：把第2章移到第4章后面
预期结果：[1, 3, 4, 2, 5]
```

### 测试用例3：跨卷移动
```
初始状态：
  第1卷：[1, 2, 3]
  第2卷：[1, 2]
操作：把第1卷第2章移到第2卷开头
预期结果：
  第1卷：[1, 3]（重新编号）
  第2卷：[2, 1, 2]（原第2章变成新第1章）
```

### 测试用例4：合并章节
```
初始状态：第1卷有5章 [1, 2, 3, 4, 5]
操作：合并第2、3、4章
预期结果：第1卷有3章 [1, 合并章节, 5]
```

### 测试用例5：错误处理
```
操作1：移动到不存在的位置
预期：返回错误提示

操作2：合并不连续的章节
预期：返回错误提示

操作3：合并单个章节
预期：返回错误提示
```

---

## 🚀 未来扩展

### 移动章节的扩展功能
1. **批量移动**：一次移动多个章节
2. **相对移动**："把这一章往后移两章"
3. **智能排序**：AI根据情节逻辑建议最佳顺序

### 合并章节的扩展功能
1. **智能合并**：AI分析内容后智能合并
2. **选择性合并**：保留某些段落，删除其他
3. **跨卷合并**：合并不同卷的章节（需要先移动再合并）

---

## 📊 代码位置

- **ApiClient.java**: 系统提示词更新（第136-278行）
- **AgentCommandExecutor.java**: 
  - `handleMoveChapter()` 方法（第510-636行）
  - `handleMergeChapters()` 方法（第641-751行）
  - switch-case 添加新命令（第60-63行）

---

## ✅ 完成清单

- [x] 更新 ApiClient 系统提示词
- [x] 实现 handleMoveChapter 方法
- [x] 实现 handleMergeChapters 方法
- [x] 在 switch-case 中添加新命令处理
- [x] 验证编译无错误
- [x] 编写使用文档

下一步建议：
- [ ] 在实际环境中测试移动功能
- [ ] 在实际环境中测试合并功能
- [ ] 添加UI手动触发入口（可选）
- [ ] 实现更多合并策略（可选）
